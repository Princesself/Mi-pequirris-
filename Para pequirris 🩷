<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para mi pequirris ❤️</title>
    <style>
        :root { --pink: #ff4d6d; --light-pink: #ff85a1; --bg: #fff0f3; }
        body { display: flex; justify-content: center; align-items: center; height: 100vh; background: var(--bg); margin: 0; font-family: 'Segoe UI', sans-serif; overflow: hidden; }
        
        .envelope-wrapper { position: relative; cursor: pointer; transition: 0.5s; width: 300px; height: 200px; }
        .envelope { position: relative; width: 100%; height: 100%; background: #f08080; box-shadow: 0 15px 35px rgba(0,0,0,0.1); z-index: 2; }
        
        /* Tapa del sobre */
        .envelope::before { content: ""; position: absolute; top: 0; z-index: 4; border-top: 130px solid #f08080; border-left: 150px solid transparent; border-right: 150px solid transparent; transform-origin: top; transition: all 0.5s ease; }
        
        /* Lados del sobre */
        .envelope::after { content: ""; position: absolute; z-index: 3; width: 0; height: 0; border-top: 100px solid transparent; border-right: 150px solid #ffadad; border-bottom: 100px solid #ffadad; border-left: 150px solid #ffadad; }

        .letter { position: absolute; bottom: 10px; left: 10px; width: 280px; height: 180px; background: white; text-align: center; display: flex; flex-direction: column; justify-content: center; align-items: center; padding: 25px; box-sizing: border-box; transition: all 0.6s ease; z-index: 1; border-radius: 5px; box-shadow: 0 0 10px rgba(0,0,0,0.05); }
        .letter p { font-size: 14px; color: #c9184a; margin: 5px 0; line-height: 1.5; font-weight: 500; }

        /* Animación de apertura */
        .open .envelope::before { transform: rotateX(180deg); z-index: 0; }
        .open .letter { transform: translateY(-150px); height: 380px; z-index: 5; box-shadow: 0 10px 20px rgba(0,0,0,0.1); }

        /* Corazones flotantes */
        .heart { position: absolute; color: var(--pink); animation: float 3s linear infinite; opacity: 0; z-index: 10; pointer-events: none; }
        @keyframes float { 0% { transform: translateY(0) scale(1); opacity: 1; } 100% { transform: translateY(-100vh) scale(1.5); opacity: 0; } }

        .hint { position: absolute; bottom: -60px; width: 100%; text-align: center; color: var(--pink); font-weight: bold; animation: bounce 2s infinite; }
        @keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
    </style>
</head>
<body onclick="abrirSorpresa()">

    <audio id="musica" loop>
        <source src="https://files.catbox.moe/kivm6o.mp3" type="audio/mpeg">
    </audio>

    <div class="envelope-wrapper" id="container">
        <div class="envelope"></div>
        <div class="letter">
            <img src="https://i.pinimg.com/originals/6c/bf/28/6cbf28264b871dac7f1eac409ea03bfd.gif" width="60">
            <p><strong>Para: Mi pequirris ❤️</strong></p>
            <p>Eres el mejor novio del mundo. Gracias por tu calma y por iluminar mis días con tu sonrisa.</p>
            <p>Quiero estar contigo hoy y siempre. Me haces inmensamente feliz. 🩷💋</p>
            <img src="https://i.pinimg.com/originals/9b/1f/f0/9b1ff0a4ea9d3461331afceeea910029.gif" width="50">
        </div>
        <div class="hint">¡Toca aquí, mi pequirris! 👇</div>
    </div>

    <script>
        let abierta = false;
        function abrirSorpresa() {
            if(!abierta) {
                document.getElementById('container').classList.add('open');
                document.getElementById('musica').play();
                lanzarCorazones();
                abierta = true;
            }
        }

        function lanzarCorazones() {
            setInterval(() => {
                const heart = document.createElement('div');
                heart.classList.add('heart');
                heart.innerHTML = '❤️';
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.top = '100vh';
                heart.style.fontSize = (Math.random() * 20 + 15) + 'px';
                heart.style.animationDuration = (Math.random() * 2 + 2) + 's';
                document.body.appendChild(heart);
                setTimeout(() => heart.remove(), 3000);
            }, 300);
        }
    </script>
</body>
</html>
